name: Release

on:
  workflow_call:
    inputs:
      tag_name:
        description: Version tag to build and publish assets for
        required: true
        type: string
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Version tag (e.g. v1.2.3)"
        required: true
        type: string

permissions:
  contents: write

env:
  APP_NAME: clockify-jira-sync

jobs:
  build:
    name: Build ${{ matrix.asset_id }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: darwin/universal
            asset_id: macos-universal
          - os: windows-latest
            platform: windows/amd64
            asset_id: windows-amd64
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag_name }}

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci --no-audit --no-fund

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.11.0

      - name: Build (macOS)
        if: runner.os == 'macOS'
        run: wails build -clean -platform "${{ matrix.platform }}"

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: wails build -clean -platform "${{ matrix.platform }}"

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: |
          set -euo pipefail
          mkdir -p release-assets
          APP_PATH="build/bin/clockify-jira-sync.app"
          if [ ! -d "$APP_PATH" ]; then
            echo "::error::App bundle not found at $APP_PATH"
            ls -la build/bin/ || true
            exit 1
          fi
          ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" \
            "release-assets/${APP_NAME}-${{ inputs.tag_name }}-${{ matrix.asset_id }}.zip"

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          New-Item -ItemType Directory -Force -Path release-assets | Out-Null
          if (-not (Test-Path "build/bin")) {
            throw "build/bin not found after Wails build."
          }
          Compress-Archive -Path "build/bin/*" `
            -DestinationPath "release-assets/$env:APP_NAME-${{ inputs.tag_name }}-${{ matrix.asset_id }}.zip" -Force

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.asset_id }}
          path: release-assets/*.zip
          if-no-files-found: error
          retention-days: 14

  publish:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [build]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag_name }}

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: assets
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd assets
          sha256sum *.zip > "${APP_NAME}-${{ inputs.tag_name }}-SHA256SUMS.txt"

      - name: Extract changelog for this version
        id: changelog
        run: |
          TAG="${{ inputs.tag_name }}"
          VERSION="${TAG#v}"
          # Extract section for this version from CHANGELOG.md
          NOTES=$(awk "/^## \[${VERSION}\]/{found=1; next} /^## \[/{if(found) exit} found{print}" CHANGELOG.md)
          if [ -z "$NOTES" ]; then
            echo "No changelog entry found, using auto-generated notes"
            echo "auto=true" >> "$GITHUB_OUTPUT"
          else
            echo "auto=false" >> "$GITHUB_OUTPUT"
            # Write notes to a file (avoids escaping issues)
            echo "$NOTES" > /tmp/release-notes.md
          fi

      - name: Create release with assets
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          TAG="${{ inputs.tag_name }}"
          # Remove any existing release for this tag (e.g. draft leftovers)
          gh release delete "$TAG" --yes 2>/dev/null || true
          # Create the release with all assets attached
          if [ "${{ steps.changelog.outputs.auto }}" = "true" ]; then
            gh release create "$TAG" assets/*.zip assets/*SHA256SUMS.txt \
              --title "$TAG" --generate-notes --latest
          else
            gh release create "$TAG" assets/*.zip assets/*SHA256SUMS.txt \
              --title "$TAG" --notes-file /tmp/release-notes.md --latest
          fi
